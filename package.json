{
  "name": "mithril-leaflet",
  "version": "0.0.1",
  "private": true,
  "description": "A map component for mithril based on leaflet.",
  "scripts": {
    "postinstall": "lerna run link",
    "link": "lerna run link",
    "clean:local": "rimraf ./docs",
    "clean": "npm run clean:local && lerna run clean --parallel",
    "build:domain": "npm run clean && lerna run build:domain --parallel",
    "start": "lerna run start --parallel",
    "patch-release": "lerna run patch-release --parallel",
    "minor-release": "lerna run minor-release --parallel"
  },
  "keywords": [
    "mithril",
    "mithril-leaflet"
  ],
  "author": "Erik Vullings <erik.vullings@gmail.com> (http://www.tno.nl)",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.16.4",
    "rimraf": "^3.0.0"
  }
}
